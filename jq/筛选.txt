1、次级节点：
	.children(‘筛选条件’):查找本元素的所有子元素(儿子)
	.find('筛选条件')：查找本元素的所有子元素(儿子、孙子。。。)
2、父级节点
	.parent('筛选条件')：取得一个包含着所有匹配的唯一上一级父元素
	.parents('筛选条件')：取得一个包含着所有匹配的所有父元素（不包含根元素）
3、兄弟节点
	.next():同级下一个
	.nextALL():同级下所有
	.prev():同级上一个
	.prevAll():同级上所有
	.siblings():同级所有除了自己